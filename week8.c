// week 8
// Linked List
// เป็นโครงสร้างข้อมูลเชิงเส้นเช่นเดียวกับ array
// แต่ linked list จะไม่ถูกจัดเก็บในตำแหน่งที่ต่อเนื่องกัน elements แต่ละตัวมีการเชื่อมโยงกันโดยใช้ pointer
// Structure เป็น การกำหนดรูปแบบหรือโครงสร้างข้อมูลที่มีการรวบรวมตัวแปรที่เก็บข้อมูลหลายเรื่องที่สัมพันธ์กัน
// เช่น
struct  student
{
    char id[12];
    char name[40];
    int age;
};
